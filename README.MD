# Commands

#### Upgrade

```
   sudo apt-get update
   sudo apt-get upgrade
```   

#### Rebot/Shutdown  

```
   sudo reboot
   sudo shutdown -h now
```

#### Install/Remove

```
   sudo get-apt install
   sudo get-apt remove
```
